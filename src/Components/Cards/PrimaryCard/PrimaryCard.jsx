import './PrimaryCard.css'

const PrimaryCard = () => {
    // const { imageLink, heading1, paragraph, style,  backgroundColor } = data;
    // console.log(data);
    return (
        <div className={` flex flex-col gap-3 p-8 text-[#fff] bg-[] primaryCardBorder rounded-lg`}>
            
        </div>
    );
};

export default PrimaryCard;